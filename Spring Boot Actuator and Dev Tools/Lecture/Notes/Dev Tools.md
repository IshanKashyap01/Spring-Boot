# Spring Boot Dev Tools

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-devtools</artifactId>
</dependency>
```

- It auto-restarts the server whenever a change is detected in the class path

- It also supports live-reload of the front-end but you have to enable the
*LiveReload* extension in Chrome
